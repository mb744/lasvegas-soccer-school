# Expo / React Native
node_modules/
.expo/
dist/
web-build/
*.orig.*

# Native
ios/
android/

# Metro
.metro-health-check*

# Env / secrets
.env*.local

# iOS signing credentials — private key + provisioning profile + password JSON
# NEVER commit any of these; they're generated locally per-machine.
credentials.json
credentials/
*.p12
*.mobileprovision

# macOS
.DS_Store

# EAS
expo-env.d.ts
